#
# Build, test and (on main/tags) publish the extension binaries.
#
# Architectures we explicitly exclude:
#   windows_amd64_mingw -- vcpkg's OpenSSL port on MinGW is fragile; sibling
#     extensions (erpl-web, quack-oauth, anofox-tabular) also skip it.
#   wasm_* -- the extension's whole purpose is HTTPS calls to the Drive API,
#     and the OAuth redirect flow needs a loopback server. Neither is viable
#     in a browser sandbox. A clean "not available on wasm" beats shipping an
#     artifact that builds green and then fails to LOAD (symbol resolution in
#     a side-module is deferred to load time, so CI would not catch it).
#
# Build matrix: current stable (v1.5.3) and the 1.4 LTS line. The LTS jobs
# build DuckDB *source* v1.4.4 but with the v1.5.3 extension-ci-tools
# workflow, because the @v1.4.4 workflow leaves the Windows compiler to
# autodetect and current windows-latest images pick MinGW g++ -- which trips
# the C++17 std::byte vs Win-SDK `byte` ambiguity and cannot link the
# MSVC-built vcpkg OpenSSL. The v1.5.3 workflow pins MSVC `cl`.
#
name: Main Extension Distribution Pipeline
on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.head_ref || '' }}-${{ github.base_ref || '' }}-${{ github.ref != 'refs/heads/main' && github.sha || '' }}
  cancel-in-progress: true

jobs:
  duckdb-stable-build:
    name: Build extension binaries (v1.5.3)
    uses: duckdb/extension-ci-tools/.github/workflows/_extension_distribution.yml@v1.5.3
    with:
      duckdb_version: v1.5.3
      ci_tools_version: v1.5.3
      extension_name: gdrive
      exclude_archs: 'windows_amd64_mingw;wasm_mvp;wasm_eh;wasm_threads'

  duckdb-lts-build:
    name: Build extension binaries (v1.4.4 LTS)
    uses: duckdb/extension-ci-tools/.github/workflows/_extension_distribution.yml@v1.5.3
    with:
      duckdb_version: v1.4.4
      ci_tools_version: v1.5.3
      extension_name: gdrive
      exclude_archs: 'windows_amd64_mingw;wasm_mvp;wasm_eh;wasm_threads'
